const balance = 100;

function sumBalance(value) {
  return balance + value;
}

console.log(sumBalance(0.02));
////////////////////////////////

function subBalance(value) {
  return balance - value;
}

console.log(subBalance(2));
////////////////////////////////

function multBalance(value) {
  return balance * (value);
}

console.log(multBalance(2));
////////////////////////////////

function divBalance(value) {
  return balance / value;
}

console.log(divBalance(2));
////////////////////////////////


//Fiz por conta própria, finalmente, estou feliz mas ainda nao acabou.
