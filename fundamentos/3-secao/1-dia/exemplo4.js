'condicao' ? 'retorno se verdadeira' : 'retorno se falsa'

let temperature = 35
let weather = temperature > 38 ? 'ta quente' : 'ta de boa';

console.log(`A temperatura e ${temperature}, portanto, ${weather}.`);
